import { StripesDirective } from './stripes.directive';

describe('StripesDirective', () => {
  it('should create an instance', () => {
    const directive = new StripesDirective();
    expect(directive).toBeTruthy();
  });
});

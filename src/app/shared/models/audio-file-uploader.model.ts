import { FileUploader, FileUploaderOptions } from 'ng2-file-upload';

export class AudioFileUploader extends FileUploader {

    constructor(options: FileUploaderOptions) {
        super(options);
    }
    
}
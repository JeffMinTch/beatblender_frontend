import { AudioManagerComponent } from './audio-manager.component';
import { Routes } from '@angular/router';
export const AudioManagerRoutes: Routes = [
    {
      path: '',
      component: AudioManagerComponent,
    }
  ]